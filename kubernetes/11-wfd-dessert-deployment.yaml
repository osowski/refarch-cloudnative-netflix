apiVersion: extensions/v1beta1
kind: Deployment
metadata:
 name: wfd-dessert
spec:
  template:
    metadata:
      labels:
        app: whats-for-dinner
        tier: app
        component: dessert
    spec:
      containers:
      - name: dessert
        image: registry.ng.bluemix.net/whats_for_dinner/wfd-dessert:V1
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: config
          mountPath: /config
      volumes:
      - name: config
        configMap:
          name: wfd-dessert
