apiVersion: extensions/v1beta1
kind: Deployment
metadata:
 name: wfd-entree
spec:
  template:
    metadata:
      labels:
        app: whats-for-dinner
        tier: app
        component: entree
    spec:
      containers:
      - name: entree
        image: registry.ng.bluemix.net/whats_for_dinner/wfd-entree:V1
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: config
          mountPath: /config
      volumes:
      - name: config
        configMap:
          name: wfd-entree
